#include <iostream>
#include<cstdlib>
#include<ctime>
using namespace std;

int *arr (int a[], int cap){
	for(int i = 0; i < cap; i++){
		a[i]=a[i]+2;
	}
	
	return a;
}


int main(){
str.rfind(key) finds the occurrence of string
key in string str.
string str = “A politician is an animal who can
sit on a fence and yet keep both ears to the
ground.”
string key = ”ears”
int found = str.rfind(key);
cout << found << endl;
	
	return 0;
}
